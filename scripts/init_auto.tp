#!/bin/bash
renice -n -10 -p $$
[[ $(echo $(st cap capdtm getusr MONITOROUT) | grep LCD) > ""  ]] && /opt/home/scripts/popup_timeout  " [ Mod v2.07${s} by Otto / KS ] " 3 &
systemctl set-environment fps=${fps}
sysctl fs.file-max=30000
